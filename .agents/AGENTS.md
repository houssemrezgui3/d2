# Project Rules

- Only operate in **Planning Mode** and create an implementation plan when explicitly requested by the user.
- For direct instructions or simple task requests, execute code changes directly without creating an implementation plan or blocking for extra approval.
- Skip browser testing
- **At the start of every new conversation, read `c:\Users\dell\d2\PROJECT_CONTEXT.md` first before opening any other file.** This file contains the project identity, tech stack, all key file paths, current `<head>` state, and the full work log. Do not explore the directory or read `index.html` until you have read this file.
- After completing any task, update the "Work Log" and "Open Items" sections of `PROJECT_CONTEXT.md` to reflect the new state.